// Vercel AI SDK — Integration helper for beepack
// Points to the real package: https://github.com/vercel/ai
// The AI Toolkit for TypeScript. Streaming, tool calling, agents. Supports OpenAI, Anthropic, Google, and more LLM providers.

/**
 * Llm Streaming — see https://github.com/vercel/ai for full documentation.
 * @param {object} config - Configuration object
 * @returns {Promise<object>} Result
 */
export async function llm_streaming(config) {
  // This is a reference integration pointing to vercel/ai.
  // For production usage, install the real package and follow the official docs.
  throw new Error("llm_streaming requires the real Vercel AI SDK package. Install from: https://github.com/vercel/ai");
}
/**
 * Tool Calling — see https://github.com/vercel/ai for full documentation.
 * @param {object} config - Configuration object
 * @returns {Promise<object>} Result
 */
export async function tool_calling(config) {
  // This is a reference integration pointing to vercel/ai.
  // For production usage, install the real package and follow the official docs.
  throw new Error("tool_calling requires the real Vercel AI SDK package. Install from: https://github.com/vercel/ai");
}
/**
 * Agent Framework — see https://github.com/vercel/ai for full documentation.
 * @param {object} config - Configuration object
 * @returns {Promise<object>} Result
 */
export async function agent_framework(config) {
  // This is a reference integration pointing to vercel/ai.
  // For production usage, install the real package and follow the official docs.
  throw new Error("agent_framework requires the real Vercel AI SDK package. Install from: https://github.com/vercel/ai");
}
/**
 * Multi Provider — see https://github.com/vercel/ai for full documentation.
 * @param {object} config - Configuration object
 * @returns {Promise<object>} Result
 */
export async function multi_provider(config) {
  // This is a reference integration pointing to vercel/ai.
  // For production usage, install the real package and follow the official docs.
  throw new Error("multi_provider requires the real Vercel AI SDK package. Install from: https://github.com/vercel/ai");
}

/**
 * Get setup instructions for Vercel AI SDK.
 * @returns {string} Installation and configuration guide
 */
export function getSetupGuide() {
  return `# Vercel AI SDK Setup

## Installation
See https://github.com/vercel/ai for installation instructions.

## Environment Variables
- OPENAI_API_KEY

## Quick Start
Visit https://github.com/vercel/ai#readme for the official quick start guide.
`;
}
