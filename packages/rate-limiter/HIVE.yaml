name: rate-limiter
version: 1.0.0
displayName: Rate Limiter with Retry
description: In-memory rate limiter with sliding window, token bucket, and automatic retry with exponential backoff
keywords:
  - rate-limit
  - throttle
  - backoff
  - retry
  - sliding-window
  - token-bucket
capabilities:
  - sliding-window-rate-limit
  - token-bucket-rate-limit
  - exponential-backoff-retry
  - per-key-rate-limiting
requires:
  deps:
    - node >= 18
compatible:
  - cursor
  - copilot
  - claude
  - openclaw
  - windsurf
language: javascript
module: esm
dependencies: none
