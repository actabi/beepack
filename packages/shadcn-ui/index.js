// shadcn/ui — Integration helper for beepack
// Points to the real package: https://github.com/shadcn-ui/ui
// Beautifully-designed, accessible UI components. Copy-paste into your project, not a dependency. 100% customizable with Tailwind CSS.

/**
 * Ui Components — see https://github.com/shadcn-ui/ui for full documentation.
 * @param {object} config - Configuration object
 * @returns {Promise<object>} Result
 */
export async function ui_components(config) {
  // This is a reference integration pointing to shadcn-ui/ui.
  // For production usage, install the real package and follow the official docs.
  throw new Error("ui_components requires the real shadcn/ui package. Install from: https://github.com/shadcn-ui/ui");
}
/**
 * Form Elements — see https://github.com/shadcn-ui/ui for full documentation.
 * @param {object} config - Configuration object
 * @returns {Promise<object>} Result
 */
export async function form_elements(config) {
  // This is a reference integration pointing to shadcn-ui/ui.
  // For production usage, install the real package and follow the official docs.
  throw new Error("form_elements requires the real shadcn/ui package. Install from: https://github.com/shadcn-ui/ui");
}
/**
 * Data Display — see https://github.com/shadcn-ui/ui for full documentation.
 * @param {object} config - Configuration object
 * @returns {Promise<object>} Result
 */
export async function data_display(config) {
  // This is a reference integration pointing to shadcn-ui/ui.
  // For production usage, install the real package and follow the official docs.
  throw new Error("data_display requires the real shadcn/ui package. Install from: https://github.com/shadcn-ui/ui");
}
/**
 * Navigation — see https://github.com/shadcn-ui/ui for full documentation.
 * @param {object} config - Configuration object
 * @returns {Promise<object>} Result
 */
export async function navigation(config) {
  // This is a reference integration pointing to shadcn-ui/ui.
  // For production usage, install the real package and follow the official docs.
  throw new Error("navigation requires the real shadcn/ui package. Install from: https://github.com/shadcn-ui/ui");
}

/**
 * Get setup instructions for shadcn/ui.
 * @returns {string} Installation and configuration guide
 */
export function getSetupGuide() {
  return `# shadcn/ui Setup

## Installation
See https://github.com/shadcn-ui/ui for installation instructions.

## Environment Variables
No environment variables required.

## Quick Start
Visit https://github.com/shadcn-ui/ui#readme for the official quick start guide.
`;
}
