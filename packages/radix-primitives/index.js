// Radix UI Primitives — Integration helper for beepack
// Points to the real package: https://github.com/radix-ui/primitives
// Open-source headless UI component library for building accessible design systems. The foundation of shadcn/ui.

/**
 * Dialog — see https://github.com/radix-ui/primitives for full documentation.
 * @param {object} config - Configuration object
 * @returns {Promise<object>} Result
 */
export async function dialog(config) {
  // This is a reference integration pointing to radix-ui/primitives.
  // For production usage, install the real package and follow the official docs.
  throw new Error("dialog requires the real Radix UI Primitives package. Install from: https://github.com/radix-ui/primitives");
}
/**
 * Dropdown Menu — see https://github.com/radix-ui/primitives for full documentation.
 * @param {object} config - Configuration object
 * @returns {Promise<object>} Result
 */
export async function dropdown_menu(config) {
  // This is a reference integration pointing to radix-ui/primitives.
  // For production usage, install the real package and follow the official docs.
  throw new Error("dropdown_menu requires the real Radix UI Primitives package. Install from: https://github.com/radix-ui/primitives");
}
/**
 * Popover — see https://github.com/radix-ui/primitives for full documentation.
 * @param {object} config - Configuration object
 * @returns {Promise<object>} Result
 */
export async function popover(config) {
  // This is a reference integration pointing to radix-ui/primitives.
  // For production usage, install the real package and follow the official docs.
  throw new Error("popover requires the real Radix UI Primitives package. Install from: https://github.com/radix-ui/primitives");
}
/**
 * Tooltip — see https://github.com/radix-ui/primitives for full documentation.
 * @param {object} config - Configuration object
 * @returns {Promise<object>} Result
 */
export async function tooltip(config) {
  // This is a reference integration pointing to radix-ui/primitives.
  // For production usage, install the real package and follow the official docs.
  throw new Error("tooltip requires the real Radix UI Primitives package. Install from: https://github.com/radix-ui/primitives");
}

/**
 * Get setup instructions for Radix UI Primitives.
 * @returns {string} Installation and configuration guide
 */
export function getSetupGuide() {
  return `# Radix UI Primitives Setup

## Installation
See https://github.com/radix-ui/primitives for installation instructions.

## Environment Variables
No environment variables required.

## Quick Start
Visit https://github.com/radix-ui/primitives#readme for the official quick start guide.
`;
}
