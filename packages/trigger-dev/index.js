// Trigger.dev — Integration helper for beepack
// Points to the real package: https://github.com/triggerdotdev/trigger.dev
// Build and deploy fully-managed AI agents and background workflows. Modern alternative to traditional job queues.

/**
 * Background Jobs — see https://github.com/triggerdotdev/trigger.dev for full documentation.
 * @param {object} config - Configuration object
 * @returns {Promise<object>} Result
 */
export async function background_jobs(config) {
  // This is a reference integration pointing to triggerdotdev/trigger.dev.
  // For production usage, install the real package and follow the official docs.
  throw new Error("background_jobs requires the real Trigger.dev package. Install from: https://github.com/triggerdotdev/trigger.dev");
}
/**
 * Scheduled Tasks — see https://github.com/triggerdotdev/trigger.dev for full documentation.
 * @param {object} config - Configuration object
 * @returns {Promise<object>} Result
 */
export async function scheduled_tasks(config) {
  // This is a reference integration pointing to triggerdotdev/trigger.dev.
  // For production usage, install the real package and follow the official docs.
  throw new Error("scheduled_tasks requires the real Trigger.dev package. Install from: https://github.com/triggerdotdev/trigger.dev");
}
/**
 * Webhook Triggers — see https://github.com/triggerdotdev/trigger.dev for full documentation.
 * @param {object} config - Configuration object
 * @returns {Promise<object>} Result
 */
export async function webhook_triggers(config) {
  // This is a reference integration pointing to triggerdotdev/trigger.dev.
  // For production usage, install the real package and follow the official docs.
  throw new Error("webhook_triggers requires the real Trigger.dev package. Install from: https://github.com/triggerdotdev/trigger.dev");
}
/**
 * Workflow Orchestration — see https://github.com/triggerdotdev/trigger.dev for full documentation.
 * @param {object} config - Configuration object
 * @returns {Promise<object>} Result
 */
export async function workflow_orchestration(config) {
  // This is a reference integration pointing to triggerdotdev/trigger.dev.
  // For production usage, install the real package and follow the official docs.
  throw new Error("workflow_orchestration requires the real Trigger.dev package. Install from: https://github.com/triggerdotdev/trigger.dev");
}

/**
 * Get setup instructions for Trigger.dev.
 * @returns {string} Installation and configuration guide
 */
export function getSetupGuide() {
  return `# Trigger.dev Setup

## Installation
See https://github.com/triggerdotdev/trigger.dev for installation instructions.

## Environment Variables
- TRIGGER_API_KEY
- TRIGGER_API_URL

## Quick Start
Visit https://github.com/triggerdotdev/trigger.dev#readme for the official quick start guide.
`;
}
