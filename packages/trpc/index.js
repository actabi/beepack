// tRPC — Integration helper for beepack
// Points to the real package: https://github.com/trpc/trpc
// End-to-end typesafe APIs made easy. The client knows the server types. Perfect for full-stack TypeScript monorepos.

/**
 * Typesafe Api — see https://github.com/trpc/trpc for full documentation.
 * @param {object} config - Configuration object
 * @returns {Promise<object>} Result
 */
export async function typesafe_api(config) {
  // This is a reference integration pointing to trpc/trpc.
  // For production usage, install the real package and follow the official docs.
  throw new Error("typesafe_api requires the real tRPC package. Install from: https://github.com/trpc/trpc");
}
/**
 * Query Mutation — see https://github.com/trpc/trpc for full documentation.
 * @param {object} config - Configuration object
 * @returns {Promise<object>} Result
 */
export async function query_mutation(config) {
  // This is a reference integration pointing to trpc/trpc.
  // For production usage, install the real package and follow the official docs.
  throw new Error("query_mutation requires the real tRPC package. Install from: https://github.com/trpc/trpc");
}
/**
 * Subscriptions — see https://github.com/trpc/trpc for full documentation.
 * @param {object} config - Configuration object
 * @returns {Promise<object>} Result
 */
export async function subscriptions(config) {
  // This is a reference integration pointing to trpc/trpc.
  // For production usage, install the real package and follow the official docs.
  throw new Error("subscriptions requires the real tRPC package. Install from: https://github.com/trpc/trpc");
}
/**
 * Middleware — see https://github.com/trpc/trpc for full documentation.
 * @param {object} config - Configuration object
 * @returns {Promise<object>} Result
 */
export async function middleware(config) {
  // This is a reference integration pointing to trpc/trpc.
  // For production usage, install the real package and follow the official docs.
  throw new Error("middleware requires the real tRPC package. Install from: https://github.com/trpc/trpc");
}

/**
 * Get setup instructions for tRPC.
 * @returns {string} Installation and configuration guide
 */
export function getSetupGuide() {
  return `# tRPC Setup

## Installation
See https://github.com/trpc/trpc for installation instructions.

## Environment Variables
No environment variables required.

## Quick Start
Visit https://github.com/trpc/trpc#readme for the official quick start guide.
`;
}
