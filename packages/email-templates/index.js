// Email Templates — Integration helper for beepack
// Points to the real package: https://github.com/forwardemail/email-templates
// Create, preview, and send custom email templates for Node.js. Supports multiple template engines and preview in browser.

/**
 * Render Templates — see https://github.com/forwardemail/email-templates for full documentation.
 * @param {object} config - Configuration object
 * @returns {Promise<object>} Result
 */
export async function render_templates(config) {
  // This is a reference integration pointing to forwardemail/email-templates.
  // For production usage, install the real package and follow the official docs.
  throw new Error("render_templates requires the real Email Templates package. Install from: https://github.com/forwardemail/email-templates");
}
/**
 * Preview Emails — see https://github.com/forwardemail/email-templates for full documentation.
 * @param {object} config - Configuration object
 * @returns {Promise<object>} Result
 */
export async function preview_emails(config) {
  // This is a reference integration pointing to forwardemail/email-templates.
  // For production usage, install the real package and follow the official docs.
  throw new Error("preview_emails requires the real Email Templates package. Install from: https://github.com/forwardemail/email-templates");
}
/**
 * Send Emails — see https://github.com/forwardemail/email-templates for full documentation.
 * @param {object} config - Configuration object
 * @returns {Promise<object>} Result
 */
export async function send_emails(config) {
  // This is a reference integration pointing to forwardemail/email-templates.
  // For production usage, install the real package and follow the official docs.
  throw new Error("send_emails requires the real Email Templates package. Install from: https://github.com/forwardemail/email-templates");
}
/**
 * Multiple Engines — see https://github.com/forwardemail/email-templates for full documentation.
 * @param {object} config - Configuration object
 * @returns {Promise<object>} Result
 */
export async function multiple_engines(config) {
  // This is a reference integration pointing to forwardemail/email-templates.
  // For production usage, install the real package and follow the official docs.
  throw new Error("multiple_engines requires the real Email Templates package. Install from: https://github.com/forwardemail/email-templates");
}

/**
 * Get setup instructions for Email Templates.
 * @returns {string} Installation and configuration guide
 */
export function getSetupGuide() {
  return `# Email Templates Setup

## Installation
See https://github.com/forwardemail/email-templates for installation instructions.

## Environment Variables
- SMTP_HOST
- SMTP_PORT
- SMTP_USER
- SMTP_PASS

## Quick Start
Visit https://github.com/forwardemail/email-templates#readme for the official quick start guide.
`;
}
