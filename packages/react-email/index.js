// React Email — Integration helper for beepack
// Points to the real package: https://github.com/resend/react-email
// Build and send emails using React components. Beautiful transactional emails with JSX instead of HTML tables.

/**
 * Render Email Html — see https://github.com/resend/react-email for full documentation.
 * @param {object} config - Configuration object
 * @returns {Promise<object>} Result
 */
export async function render_email_html(config) {
  // This is a reference integration pointing to resend/react-email.
  // For production usage, install the real package and follow the official docs.
  throw new Error("render_email_html requires the real React Email package. Install from: https://github.com/resend/react-email");
}
/**
 * React Components — see https://github.com/resend/react-email for full documentation.
 * @param {object} config - Configuration object
 * @returns {Promise<object>} Result
 */
export async function react_components(config) {
  // This is a reference integration pointing to resend/react-email.
  // For production usage, install the real package and follow the official docs.
  throw new Error("react_components requires the real React Email package. Install from: https://github.com/resend/react-email");
}
/**
 * Email Preview — see https://github.com/resend/react-email for full documentation.
 * @param {object} config - Configuration object
 * @returns {Promise<object>} Result
 */
export async function email_preview(config) {
  // This is a reference integration pointing to resend/react-email.
  // For production usage, install the real package and follow the official docs.
  throw new Error("email_preview requires the real React Email package. Install from: https://github.com/resend/react-email");
}
/**
 * Responsive Templates — see https://github.com/resend/react-email for full documentation.
 * @param {object} config - Configuration object
 * @returns {Promise<object>} Result
 */
export async function responsive_templates(config) {
  // This is a reference integration pointing to resend/react-email.
  // For production usage, install the real package and follow the official docs.
  throw new Error("responsive_templates requires the real React Email package. Install from: https://github.com/resend/react-email");
}

/**
 * Get setup instructions for React Email.
 * @returns {string} Installation and configuration guide
 */
export function getSetupGuide() {
  return `# React Email Setup

## Installation
See https://github.com/resend/react-email for installation instructions.

## Environment Variables
No environment variables required.

## Quick Start
Visit https://github.com/resend/react-email#readme for the official quick start guide.
`;
}
